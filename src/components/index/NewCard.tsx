
type NewCardProps = {
    title: string,
    description : string,
    author : string,
    profile : string
}

export default function NewCard({title, description, author, profile}:NewCardProps) {
    return <div></div>
}